# ansible4cmdbuild

Ansible-based inventory sync for CMDBuild.

## Structure

- `ansible/` - playbooks and inventory collection tasks
- `config/` - sample configuration files
- `scripts/` - bash scripts for export and sync
- `docs/` - mapping and operational notes
